/**
 * Interface that represents all usable objects in this world.
 */
interface Usable {
    /**
     * use Function to be implemented for all entities that implements this
     * interface.
     */
    void use();
}
